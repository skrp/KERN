#########################
# KERN - unix kernel
  [0] KERN.CONF 
  [1] buildworld
  [2] jail
  [3] pf 
#########################
# FREEBSD ANONI JAIL GATE
ANONI - user anonymous jail
# external
usr@ip4 => host_ip4 => ANONI
usr-> OTOPSK -> service
usr <- OTOPSK_PACKET <- service
# internal
ANONI -> service
# PROTO #################
%NET{} = $NETID_salt_hash;
@node = $NETID;
$port = $key_family[];
@key_family[] = 'range of $SKEY in 10k steps';

$NET{node[$port]} = $key_family;
